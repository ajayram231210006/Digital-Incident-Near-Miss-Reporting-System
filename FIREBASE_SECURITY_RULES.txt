{
  "rules": {
    ".read": false,
    ".write": false,

    "users": {
      ".read": "auth != null && root.child('users').child(auth.uid).child('role').val() === 'admin'",
      "$uid": {
        ".read": "auth != null && (auth.uid === $uid || root.child('users').child(auth.uid).child('role').val() === 'admin')",
        ".write": "auth != null && (auth.uid === $uid || root.child('users').child(auth.uid).child('role').val() === 'admin')",

        "role": {
          ".write": "auth != null && root.child('users').child(auth.uid).child('role').val() === 'admin'",
          ".validate": "newData.isString() && (newData.val() === 'reporter' || newData.val() === 'supervisor' || newData.val() === 'admin')"
        },

        "status": {
          ".write": "auth != null && root.child('users').child(auth.uid).child('role').val() === 'admin'",
          ".validate": "newData.isString() && (newData.val() === 'pending' || newData.val() === 'pending_approval' || newData.val() === 'active' || newData.val() === 'inactive' || newData.val() === 'rejected')"
        },

        "email": {
          ".validate": "newData.isString()"
        },

        "firstName": {
          ".validate": "newData.isString()"
        },

        "lastName": {
          ".validate": "newData.isString()"
        },

        "createdAt": {
          ".validate": "newData.isString()"
        },

        "fcmToken": {
          ".read": "auth != null && (auth.uid === $uid || root.child('users').child(auth.uid).child('role').val() === 'admin')",
          ".write": "auth != null && (auth.uid === $uid || root.child('users').child(auth.uid).child('role').val() === 'admin')",
          ".validate": "newData.val() === null || newData.isString()"
        }
      }
    },

    "incidents": {
      ".read": "auth != null",
      "$incidentId": {
        ".read": "auth != null",
        ".write": "auth != null && ((!data.exists() && newData.child('reporterUid').val() === auth.uid) || root.child('users').child(auth.uid).child('role').val() === 'supervisor' || root.child('users').child(auth.uid).child('role').val() === 'admin')",

        "createdAt": {
          ".validate": "newData.isString()"
        },

        "date": {
          ".validate": "newData.isString()"
        },

        "description": {
          ".validate": "newData.isString()"
        },

        "imageUrl": {
          ".validate": "newData.val() === null || newData.isString()"
        },

        "lastModified": {
          ".validate": "newData.val() === null || newData.isString()"
        },

        "location": {
          ".validate": "newData.isString()"
        },

        "notes": {
          ".validate": "newData.val() === null || newData.isString()"
        },

        "reporterEmail": {
          ".validate": "newData.isString()"
        },

        "reporterUid": {
          ".validate": "newData.isString() && (!data.exists() ? newData.val() === auth.uid : newData.val() === data.val())"
        },

        "severity": {
          ".validate": "newData.val() === null || (newData.isString() && (newData.val() === '' || newData.val() === 'low' || newData.val() === 'medium' || newData.val() === 'high' || newData.val() === 'critical'))"
        },

        "status": {
          ".validate": "newData.isString() && (newData.val() === 'open' || newData.val() === 'active' || newData.val() === 'closed')"
        },

        "type": {
          ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 100"
        }
      }
    },

    "incidentReadStatus": {
      "$uid": {
        ".read": "auth != null && (auth.uid === $uid || root.child('users').child(auth.uid).child('role').val() === 'admin')",
        ".write": "auth != null && (auth.uid === $uid || root.child('users').child(auth.uid).child('role').val() === 'admin')",
        "$incidentId": {
          ".validate": "newData.hasChildren(['read', 'readAt']) && newData.child('read').isBoolean() && newData.child('readAt').isString()"
        }
      }
    },

    "notificationPreferences": {
      "$uid": {
        ".read": "auth != null && (auth.uid === $uid || root.child('users').child(auth.uid).child('role').val() === 'admin')",
        ".write": "auth != null && (auth.uid === $uid || root.child('users').child(auth.uid).child('role').val() === 'admin')"
      }
    },

    "notificationTokens": {
      "$uid": {
        ".read": "auth != null && (auth.uid === $uid || root.child('users').child(auth.uid).child('role').val() === 'admin')",
        ".write": "auth != null && (auth.uid === $uid || root.child('users').child(auth.uid).child('role').val() === 'admin')",
        "token": {
          ".validate": "newData.val() === null || newData.isString()"
        },
        "updatedAt": {
          ".validate": "newData.val() === null || newData.isString()"
        }
      }
    },

    "userNotifications": {
      "$uid": {
        ".read": "auth != null && (auth.uid === $uid || root.child('users').child(auth.uid).child('role').val() === 'admin')",
        "$notificationId": {
          ".write": "auth != null && (auth.uid === $uid || root.child('users').child(auth.uid).child('role').val() === 'admin' || root.child('users').child(auth.uid).child('role').val() === 'supervisor' || (!data.exists() && root.child('users').child(auth.uid).exists()))"
        }
      }
    }
  }
}
